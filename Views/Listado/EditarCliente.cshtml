@model SistemaAlmacenamiento.Models.Cliente

@{
    ViewData["Title"] = "Editar Cliente";
}

<h1>Editar Cliente</h1>

<form asp-action="EditarCliente" method="post">
    <div class="form-group">
        <label asp-for="Ruc" class="form-label">RUC</label>
        <input asp-for="Ruc" class="form-control" maxlength="11" required />
        <span asp-validation-for="Ruc" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="RazonSocial" class="form-label">Razón Social</label>
        <input asp-for="RazonSocial" class="form-control" required />
        <span asp-validation-for="RazonSocial" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Direccion" class="form-label">Dirección</label>
        <input asp-for="Direccion" class="form-control" required />
        <span asp-validation-for="Direccion" class="text-danger"></span>
    </div>

    <!-- Fecha de creación (solo lectura) -->
    <div class="form-group">
        <label asp-for="FechaCreacion" class="form-label">Fecha de Creación</label>
        <input asp-for="FechaCreacion" class="form-control" readonly/>
    </div>
    <br>
    <button type="submit" class="btn btn-primary">Guardar Cambios</button>
    <a asp-controller="Listado" asp-action="TablaClientes" class="btn btn-secondary">Cancelar</a>
</form>
